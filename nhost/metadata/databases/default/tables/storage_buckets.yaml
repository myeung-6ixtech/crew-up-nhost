table:
  name: buckets
  schema: storage
array_relationships:
  - name: files
    using:
      foreign_key_constraint_on:
        column: bucket_id
        table:
          name: files
          schema: storage
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - download_expiration
        - max_upload_file_size
        - min_upload_file_size
      filter: {}
