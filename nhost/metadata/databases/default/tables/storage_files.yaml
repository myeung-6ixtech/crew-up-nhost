table:
  name: files
  schema: storage
# Nhost Storage queries/mutates `files` on query_root — not `storage_files`.
configuration:
  custom_name: files
  custom_root_fields:
    select: files
    select_by_pk: file
    select_aggregate: files_aggregate
    insert: insert_files
    insert_one: insert_file
    update: update_files
    update_by_pk: update_file
    delete: delete_files
    delete_by_pk: delete_file
object_relationships:
  - name: bucket
    using:
      foreign_key_constraint_on: bucket_id
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - bucket_id
        - name
        - size
        - mime_type
        - created_at
        - updated_at
        - uploaded_by_user_id
      filter:
        uploaded_by_user_id:
          _eq: X-Hasura-User-Id
insert_permissions:
  - role: user
    permission:
      check:
        uploaded_by_user_id:
          _eq: X-Hasura-User-Id
      set:
        uploaded_by_user_id: X-Hasura-User-Id
      columns:
        - bucket_id
        - name
        - size
        - mime_type
delete_permissions:
  - role: user
    permission:
      filter:
        uploaded_by_user_id:
          _eq: X-Hasura-User-Id
